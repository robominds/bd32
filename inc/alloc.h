/* Stub header for alloc.h (memory allocation) - Auto-generated */

#pragma once

#include <stdlib.h>

/* Memory allocation aliases for compatibility */
#define farmalloc(size) malloc(size)
#define farfree(ptr) free(ptr)
